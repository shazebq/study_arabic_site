// this is for the twitter bootstrap image carousel
// by default it revolves the images, this is so that it doesn't
$(document).ready(function() {
    $('.carousel').carousel( { pause: true, interval: false }); 	
})
