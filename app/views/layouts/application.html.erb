<!DOCTYPE html>
<html>
<head>
  <title><%= yield :title %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

</head>
<body>

<div class="container" id="header"><!--div for logo and navigation-->
    <div class="row">

      <div class="span12 logo_container">
        <div class="row">
          <div class="span9">
            <h2 class="white_font">StudyArabic.com</h2>
          </div>
          <div class="span3">
            <% if user_signed_in? %>
                 You are signed in as <%= current_user.email %> | <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
            <% else %>
                <%= link_to "Sign Up", new_user_registration_path %> or <%= link_to "Sign In", new_user_session_path %>
            <% end %>
          </div>
        </div>

        <div class="row">
          <div class="span8">
          <ul class="nav nav-tabs custom_nav bold">
            <li><%= link_to("Home", "/") %></a></li>
            <li class="<%= params[:controller] == "centers" ? "active_nav" : "not_active" %>"><%= link_to("Centers/Programs", centers_path) %></li>
            <li class="<%= params[:controller] == "teacher_profiles" ? "active_nav" : "not_active" %>"><%= link_to("Teachers", teacher_profiles_path) %></a></li>
            <li class="<%= params[:controller] == "resources" ? "active_nav" : "not_active" %>"><%= link_to("Resources", resources_path) %></a></li>
            <li class="<%= params[:controller] == "articles" ? "active_nav" : "not_active" %>"><a href=#>Articles</a></li>
            <li class="dropdown <%= params[:controller] == "categories" || params[:controller] == "forum_posts" ? "active_nav" : "not_active" %>">
            <a class="dropdown-toggle disabled" data-toggle="dropdown" href="<%= categories_path %>">Forums
                <b class="caret custom_caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><a href="<%= category_forum_posts_path(Category.find_by_name("Arabic Language")) if Category.find_by_name("Arabic Language") %>">Arabic Language</a></li>
                <li><a href="<%= category_forum_posts_path(Category.find_by_name("Study Abroad")) if Category.find_by_name("Study Abroad") %>">Study Abroad (General)</a></li>
                <li><a href="<%= category_forum_posts_path(Category.find_by_name("Countries")) if Category.find_by_name("Study Abroad") %>">Study Abroad (By Country)</a></li>
              </ul>
            </li>
          </ul>
          </div><!--navbar-->

        </div><!--div for nav and search box-->
          
      </div>


    </div>
</div><!--div for logo and navigation-->

<div class="container" id="main_content">

  <div class ="row">
    <div class="span12">
     <div class="row">
        <div class="span9 main_content">
          <p id="notice"><%= notice %></p>
          <p id="notice"><%= alert %></p>
          <%= yield %>
          <%= debug(params) if Rails.env.development? %> 
        </div>
        <div class="span3 sidebar">
            <%= yield :side_bar %>
            <br />
            Some ads here. Other information, etc
        </div>
      </div>
    </div>
  </div>

</div>
</body>
</html>
