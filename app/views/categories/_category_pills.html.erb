<ul class="nav nav-pills nav-stacked">
  <li class="active">
    <a href="#">Home</a>
  </li>
  <!--<p><%= category_title %></p>-->
  <% CategoryParent.find_by_name(parent_name).categories.each do |cat| %>
      <li><%= link_to(cat.name, category_forum_posts_path(cat, order_by: @current_scope))   %></li>
  <% end %>
</ul>

