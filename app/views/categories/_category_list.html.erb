<p><h4><%= link_to(category_title, category_forum_posts_path(Category.find_by_name(parent_name))) %></h4></p>
<% CategoryParent.find_by_name(parent_name).categories.each do |cat| %>
    <%=  link_to(cat.name, category_forum_posts_path(cat)) %><br />
<% end %>
