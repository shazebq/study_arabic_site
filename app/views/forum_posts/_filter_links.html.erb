<ul class="nav nav-pills custom">
  <% @scopes.each do |scope| %>
      <li class="<%= @current_scope == scope ? 'active' : 'not_active' %>"><%= link_to(scope_name_to_link_text(scope), forum_posts_path(order_by: scope)) %></li>
  <% end %>
</ul>